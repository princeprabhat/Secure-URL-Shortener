import catchAsync from "../utils/catchAsync";


const generateUrl = catchAsync(async (urlData) => {

})

const getUrlByShortCode = catchAsync(async (shortCode) => {

})

const incrementClick = catchAsync(async (shortCode) => {

})

export default {
    generateUrl,
    getUrlByShortCode,
    incrementClick
}